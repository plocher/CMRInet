// Fill in your WiFi credentials. Do NOT commit this file to version control.
// Copy to secrets.h and edit, or inject via build defines:
//   --build-property "compiler.cpp.extra_flags=-DWIFI_SSID=... -DWIFI_PASSWORD=..."
#pragma once

#define WIFI_SSID     "your-network"
#define WIFI_PASSWORD "your-password"

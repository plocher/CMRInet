# bench probe venv — recreated by setup.sh
.venv/
__pycache__/
**/data/**/*.raw
**/data/**/*.log
# Run artifacts too: manifests/summaries are regenerated by gather/analyze.
# (Historic Issue47 manifests predate this rule and stay tracked.)
**/data/**/*.json
